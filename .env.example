# Variables de docker compose (cópialo como .env si necesitas cambiarlas).
# .env está en .gitignore.

# Puerto del panel en el host (default: 8080)
# PANEL_PORT=8090

# Zona horaria del backend (nombres y fechas de los snapshots; default: UTC)
# TZ=America/Guayaquil
